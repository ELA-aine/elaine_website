const images = [...document.querySelectorAll('.imgae')];

//popup

const popup = document.querySelectorAll(' .popup');
const closeBtn = document.querySelectorAll('closebtn');
const largeIamge = document.querySelectorAll('.Sherry');